#include "types.h"

namespace dminion {
namespace globals {

extern const int kScreenWidth = 1024;
extern const int kScreenHeight = 768;
extern const int kScreenDepth = 32;

extern const Color white(255, 255, 255);
extern const Color black(0, 0, 0);

}
}
